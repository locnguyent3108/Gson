Array list sử dụng cấu trúc mảng để lưu trữ phần tử. tuy nhiên có 1 vài sự khác biệt
	1. ko cần khai báo trước các kiểu phần tử.
	2. ko cần xác định trước các số lượng phần tử 
	3. Có thể truy cập phần tử ngẫu nhien do inherit từ interface RandomAccess 
	
//--------------------------------------------------------------------------------------
Array List method: 
1. add (Object o)
2. remove (Object o)
3. get (int index)
4. size ()
5. isEmpty ()
6. contains (Object o)
7. clear()

//--------------------------------------------------------------------------------------
HashMap là kiểu tập hợp từ điển, HashMap cho phép truy xuất trực tiếp  tới 1 đối tượng
 thông qa khoá duy nhất cuả nó. Cả 2 khoá và đối tượng có thể thuộc bất kỳ kiểu nào.

//--------------------------------------------------------------------------------------
 
 HashMap method:
1. put(Object key, Object value)
2. get(Object key)
3. remove(Object key)
4. containsKey(Object key)
5. containsValue(Object value)
6. size()
7. isEmpty().